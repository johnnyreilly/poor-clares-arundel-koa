sudo: required

language: node_js

node_js: "10"

services:
  - docker

install: true

script:
  - docker build --tag johnnyreilly/poorclaresarundel .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master